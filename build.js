/**\n * 🏗️ BUILD SCRIPT\n * \n * Script de construcción para compilar TypeScript a JavaScript\n * Compatible con el entorno actual sin Vite\n */\n\nconst fs = require('fs');\nconst path = require('path');\nconst { execSync } = require('child_process');\n\n// Configuración\nconst config = {\n    srcDir: 'resources/ts',\n    outDir: 'public/dist/js',\n    entryPoints: [\n        'resources/ts/app.ts',\n        'resources/ts/htmx-config.ts'\n    ]\n};\n\n/**\n * Crea directorios si no existen\n */\nfunction ensureDirectoryExists(dirPath) {\n    if (!fs.existsSync(dirPath)) {\n        fs.mkdirSync(dirPath, { recursive: true });\n        console.log(`📁 Created directory: ${dirPath}`);\n    }\n}\n\n/**\n * Compila TypeScript\n */\nfunction compileTypeScript() {\n    console.log('🔨 Compiling TypeScript...');\n    \n    try {\n        // Compilar TypeScript\n        execSync('npx tsc', { stdio: 'inherit' });\n        console.log('✅ TypeScript compilation completed');\n    } catch (error) {\n        console.error('❌ TypeScript compilation failed:', error.message);\n        process.exit(1);\n    }\n}\n\n/**\n * Copia archivos adicionales\n */\nfunction copyAssets() {\n    console.log('📋 Copying additional assets...');\n    \n    // Crear directorio de salida\n    ensureDirectoryExists(config.outDir);\n    \n    // Aquí podrías copiar otros archivos si es necesario\n    console.log('✅ Assets copied');\n}\n\n/**\n * Minifica archivos para producción\n */\nfunction minifyForProduction() {\n    if (process.env.NODE_ENV === 'production') {\n        console.log('🗜️ Minifying for production...');\n        \n        try {\n            // Minificar archivos JavaScript\n            execSync(`npx terser ${config.outDir}/**/*.js -o ${config.outDir}/app.min.js --source-map`, { stdio: 'inherit' });\n            console.log('✅ Minification completed');\n        } catch (error) {\n            console.error('❌ Minification failed:', error.message);\n        }\n    }\n}\n\n/**\n * Función principal de build\n */\nfunction build() {\n    console.log('🚀 Starting build process...');\n    \n    // Verificar que existe tsconfig.json\n    if (!fs.existsSync('tsconfig.json')) {\n        console.error('❌ tsconfig.json not found');\n        process.exit(1);\n    }\n    \n    // Ejecutar pasos del build\n    compileTypeScript();\n    copyAssets();\n    minifyForProduction();\n    \n    console.log('🎉 Build completed successfully!');\n}\n\n/**\n * Función de watch para desarrollo\n */\nfunction watch() {\n    console.log('👀 Starting watch mode...');\n    \n    try {\n        execSync('npx tsc --watch', { stdio: 'inherit' });\n    } catch (error) {\n        console.error('❌ Watch mode failed:', error.message);\n        process.exit(1);\n    }\n}\n\n// Ejecutar según el comando\nconst command = process.argv[2];\n\nswitch (command) {\n    case 'watch':\n    case 'dev':\n        watch();\n        break;\n    case 'build':\n    case 'prod':\n        process.env.NODE_ENV = 'production';\n        build();\n        break;\n    default:\n        build();\n        break;\n}\n\nmodule.exports = { build, watch };"